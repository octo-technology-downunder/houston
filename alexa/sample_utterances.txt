Deploy deploy {application} to {environment} 
Deploy deploy {application} to the {environment} 
Deploy deploy {application} to the {environment} 
Deploy send {application} to {environment} 
Deploy send {application} to the {environment} 
Deploy deploy {application} to the {environment} environment
Deploy deploy my {application} on {environment} 
Deploy deploy my {application} to {environment} 
Deploy push {application} to {environment} 
Deploy push {application} to the {environment} 
Deploy push {application} to the {environment} environment

Confirm confirm with code {code}
Confirm confirm deployment with code {code}
Confirm confirm with {code}
Confirm confirm deployment with {code}
Confirm confirm {code}
Confirm confirm deployment {code}
